spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/my_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
      password: MyVerySecurePassword

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  # configuration cache type
  cache:
    type: redis

config:
  security:
    secret: 388c81c79cada4abd82f6cd6a1b76de8eb0c156009f98137e4c41064430eabdd
    expiration: 3600000 # ms
    refresh-token-expiration: 3 # hr
  pagination:
    base-url: http://localhost:8080/api/v1
    uri:
      product: /products/paginated
      stock: /stocks/paginated
      supplier: /suppliers/paginated
      user: /users/paginated
      order: /orders/paginated
  json-placeholder:
    base-url: https://jsonplaceholder.typicode.com
    uri:
      posts: /posts
      comments: /comments
  telegram:
    base-url: https://api.telegram.org/bot{TOKEN}/sendMessage
    token: <YOUR_TELEGRAM_TOKEN>
    chat-id: -4856996045